import React from 'react';

const NoData = () => (
    <div className="alert alert-danger rounded-0" role="alert">
        <strong>There is no data returned from Server.</strong>
    </div>
);

export default NoData;
